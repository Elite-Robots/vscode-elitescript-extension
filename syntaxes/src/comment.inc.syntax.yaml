---
comments${suffix}:
  name: comment.line.number-sign.elitescript
  begin: (\#)
  beginCaptures:
    '1': {name: punctuation.definition.comment.elitescript}
  end: ($${marker})
  patterns:
    - include: '#codetags'
...
